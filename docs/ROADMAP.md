# 🗺️ Roadmap

> 这份路线图描述「就决定是你了」的**方向与优先级，是方向声明而非交付承诺**——0.x 阶段排序会随反馈调整，Later 的事项尤其可能变化。
>
> 组织方式参考社区通行实践（Now / Next / Later 分层 + 状态图例，不绑定日期）。最后更新：2026-09-20，随版本滚动更新。

## 如何阅读

- **Now**：当前正在做的；**Next**：完成 Now 之后的计划；**Later**：方向性探索，欢迎用 issue 推动或改变排序
- 状态图例：🚧 进行中 · 📋 已排期 · 🔍 探索中 · ⬜ 待办
- 每个条目先说**要解决的问题**，再说方案——方案会变，问题不变
- 已发布功能请看 GitHub [Releases](https://github.com/Jo0OCTsai/pokemon-choose-you/releases)，本文只在文末保留近期亮点
- 想提需求、报 bug 或建议优先级？直接开 [issue](https://github.com/Jo0OCTsai/pokemon-choose-you/issues)（有 bug 报告 / 功能请求模板）——这份排序很大程度由反馈驱动

---

## 🎯 Now —— 当前焦点

### 1.0.0 首发收尾

- ⬜ **macOS 真机验证**桌宠透明/置顶（配置已就绪，唯一卡首发的硬验证项）
- ⬜ **发布工程收尾**：三平台打包链路已修通（macOS universal 双架构 lipo、Windows NSIS、Linux deb/rpm/AppImage + minisign 更新包，CI 全矩阵验证中）；剩余首发门面——demo GIF / 截图、CHANGELOG 首条、README 安装方式表格。提交规范与 release-please 已就位
- 🚧 **Windows 代码签名**：SignPath OSS 免费签名申请中；获批前首发不签名，SmartScreen 应对已写进用户指南

---

## 🔜 Next —— 接下来

> 原 Next 各项（数据安全、Agent CLI 链路增强、飞书切换 lark-cli、回顾闭环、注意力友好设计）**均已落地**，详见文末「近期已落地」；新一批优先级将从下方 Later 与社区 issue 反馈中提拔。

---

## 🌱 Later —— 方向探索

### AI 与收音机深化

- 🔍 **AI 判重三段漏斗**：字符串模糊匹配 → embedding 召回 → LLM 边界裁决，跨批次去重（Slack→Linear 实践中公认未解决的痛点）
- 🔍 **AI 处理任务深化**：基础派发已落地（手动 + 自动派发、交互/无头双通道、状态回传，见「近期已落地」）；剩余方向——完成/需输入的通知区分、通知内直接行动、摘要式审查（digest）、只拦不可逆操作、卡死兜底
- 🔍 **用量与成本统计页**：今日/7天/30天 + 按模型分组，会话级累计（对标社区事实标准 ccusage）
- 🔍 **任务时间线**：来源消息 → AI 建议 → 捕捉决定 → 跟进 → agent 执行轨迹，完整生命周期视图
- 🔍 **飞书 WebSocket 实时事件**：lark-event 订阅替代 lark-cli 轮询——长连接的断线重连 / 休眠唤醒 / 多实例生命周期管理是主要成本

### 知识库

- 🔍 **Obsidian 对接**：基于官方 CLI（1.12+）实现任务 ↔ 笔记互链、每日捕捉摘要写入 daily note、任务属性 frontmatter 同步

### 桌宠体验

- 🔍 **精灵多状态动画**：idle / 工作 / 睡觉三态 + 夜间自动入睡（目前只有 CSS 状态切换）
- 🔍 撤销语义补全（完成已支持撤销，删除/批量操作补齐）；主面板搜索/过滤
- 🔍 动效强度分档：紧急闪烁、敲门抖动、蹦跳动画可调低或整体关闭（「减少动画」开关）——紧急提示对感官敏感用户可能过载

### 外观与系统融合

- 🔍 **深色模式**：主题色跟随系统 `prefers-color-scheme` 并支持手动三档（浅色/深色/跟随系统）——现固定精灵红浅色主题，夜间使用刺眼；需整套配色令牌分档（图鉴机身红、卡片底、LCD 屏都要有暗色对应）

### 游戏化（只做正向激励）

- 🔍 **随机捕捉掉落**：完成任务有概率遇到野生宝可梦登录图鉴（可变奖励，图鉴收集率 x/151 现成展示位）；小概率闪光个体做稀有度分层——可变奖励比固定奖励更能维持动机
- 🔍 季节性限定内容（限定皮肤 / 台词 / 出没表）：常驻类应用几个月后的新鲜感衰减是留存主敌，用限定事件定期续期
- 🔍 成就徽章、每日捕捉目标喂给桌宠台词、52 周热力图 + 年度图鉴报告、专注统计单页
- 🔍 物理拖拽、8-bit 音效包
- 🔍 连胜的「周」视图汇总（日历日连胜判定与连胜演出已落地，见「近期已落地」）

### 任务模型

- 🔍 循环任务（提前完成不破坏循环）、子任务独立排期、任务模板（「远征套装」）

### 日历

- 🔍 只读 ICS 订阅导出（任务出现在系统日历；双向同步见「暂不做」）

---

## ✅ 近期已落地

- **任务系统**：标签（多打）、全字段编辑、跟进记录、查询、草丛/路线/图鉴状态机、分级提醒（后台调度不漏）
- **标签体系维度化 + AI 自动治理**：内置项目（单选）/场景/人物/主题四维度、可自定义；AI 打标按维度选标签、有明确依据时提议新标签（接受建议才创建）、每维度数量上限防碎片；词表外 `#新名字` 捕捉即建；`pk tag create` 让 agent 自助扩词表；用户移除标签的记录聚合为负反馈注入分类 prompt
- **相对截止时间（时间盲友好）**：任务卡截止显示为距现在的距离（X 分钟后 / 明天 15:00 / 周五 / 已逾期 X），按临近程度五档变色，悬停见绝对时间，设置可关
- **自然语言快速捕捉（AI 版）**：新建待办的唯一入口收拢到收音机——一句话（「明天下午 5 点交周报」）经 AI agent 判定内容/分类/标签/截止/优先级，新待办直接建好（5 秒可撤销），判重命中现有待办则生成更新建议等人工确认；冒险/路线/草丛页的创建表单与本地正则解析（nlCapture）已移除，全局快捷键 `Ctrl/Cmd+Shift+K` 直达捕捉框
- **收音机**：飞书消息全量展示与查询、60 天自动清理、强制创建（先判重）、◎ 捕捉 / ✕ 逃走人工裁决；AI 判定三动作——新待办 / 更新建议（确认后打补丁到目标待办）/ 跟进（自动并入并显示「已并入待办 No.x」）；待处理消息批量分诊（全选批量捕捉 / 逃走，单条失败不影响其余）；每条建议附**判定理由 + 置信档位（高/中/低）**，逃走可选原因码，反馈连同建议与模型落本地库（chat_feedback）
- **诊断中心**（设置 → 诊断）：集成健康面板（飞书 / AI 三档状态、上次成功、下次轮询、失败计数、待确认积压、一键重试，后台实时刷新）+ 应用内日志查看器（级别 / 来源过滤）+ 脱敏支持报告（版本 + 健康快照 + 最近日志，敏感键与 Bearer 凭证自动遮蔽）
- **AI**：agent CLI 多配置（Claude Code / OpenCode / Kiro CLI + 自定义）、`pk` CLI 供 agent 调用（task / note / log / category / tag / context，全 JSON 输出）、会话历史快捷入口
- **Agent SSH 远程执行**：agent 配置可指定 SSH 目标（host/端口/私钥），无头调用与历史入口均经 `ssh --` 以登录 shell（`$SHELL -lc`）转发——非交互会话也能找到 brew/nvm 安装的 CLI；提示词走 stdin 免遭远端 shell 重解析，BatchMode 免交互、连接超时 10 秒；远端找不到命令（退出码 127）时附 PATH 修复指引
- **会话回链与成本记录**：agent_sessions 表按次落库（session_id / 命令 / 退出码 / 成本 / 时长 / token），分类调用自动记录，`pk session log` 关联任务；任务详情抽屉展示并可回放转录
- **配套 skill 分发**：`pk skill install claude-code|opencode [--dir]` 一键装 SKILL.md（含 pk 命令速查与 agent 建议流程），`pk skill show` 输出原文
- **Agent 工具调用模式（pk suggest，现为唯一回收方式）**：agent 先 `pk context` 拿判重上下文、`pk suggest batch` 一次性把整批判定写回数据库，应用直接回读、不解析 agent 输出文本（早期的 text 文本解析模式已下线）；`pk suggest todo|update|follow-up|none|batch` 全套提交命令（todo/update 写建议列保留人工确认、follow-up 自动挂跟进，整批校验一损俱损、同消息幂等覆盖），建议落库收敛为 conn 层单一实现；技能改为渐进披露多文件（SKILL.md + references/ 参数表与批处理工作流，带版本标记、跨版本重装提示更新）；连接测试为工具探针（真跑一次 pk context，验证白名单/PATH/数据库整条链路）
- **远程 agent 用 pk（反向隧道 + 一键配置）**：`pk remote shim --host <本机> [--port] [--key] [--write]` 生成远程透传脚本，pk 命令经 ssh 回本机执行、数据始终留在本机；agent 的 SSH 配置填反向隧道端口（`-R` 把远程侧 127.0.0.1:端口 转回本机 sshd），本机零入站。设置页「一键配置远程 pk」按钮全自动完成部署——密钥生成/公钥装配/私钥推送/主机指纹预信任/shim 安装与 PATH 保障/端口写回/真实隧道端到端验证，全程幂等逐步报告（用户前置仅需开启本机 sshd）
- **pk 的 agent 友好性四件套**：`pk doctor` 环境自检（数据库存在性/schema 版本/quick_check 完整性/WAL 并发/context 读链路/技能安装版本，每项带 fix 修复建议，有 fail 退出码 1；`--ssh <host>` 端到端验证远程 shim 链路）；`task create/update/delete --dry-run` 只校验回显不落库；`task list --limit`（默认 50，truncated 时提示用 search 收窄——token 瘦身）；`pk help --json` 机器可读命令目录
- **待办派发给 Agent**：给项目标签配「派发设置」（agent / 工作目录 / 项目上下文）后，待办从详情抽屉一键派发——交互通道（本地新终端 / ssh+tmux 注入与降级）或无头通道（跑完按退出码自动回传，claude 解析信封续接会话）；`pk dispatch start/done/fail` 状态回传 + 抽屉状态徽章；完成/失败系统通知；可选自动派发（到期未开始且标签指定 agent 的待办自动排队无头执行）、每机器并发上限、worktree 隔离；任务 prompt 定界隔离防注入
- **飞书**：用户身份 OAuth 增量拉取私聊/群聊（无需拉机器人进会话）、富文本渲染、按聊天语境过滤 + 同会话 30 分钟上下文、断网退避重试
- **飞书接入（官方 lark-cli）**：拉取走官方 lark-cli（MIT）——以用户本人身份轮询，`config init` 首跑自动建自建应用、`auth login` 完成授权，凭证由 lark-cli 自管不进本库（早期内置直连引擎已下线，遗留配置与凭证启动时自动清理）。拉取覆盖 p2p 单聊（`types=p2p,group`）——与机器人应用的单聊及「发给自己的会话」里用户发的消息按备忘提取；免打扰会话整个跳过（折叠状态开放平台未暴露，以免打扰为最接近的可见信号）
- **任务详情抽屉**：点击任务卡片右侧滑出——属性总览（分类/优先级/状态/截止/标签/来源/专注/时间戳）+ 跟进记录（可直接补充）+ Agent 执行 + 操作历史；编辑弹窗瘦身为纯表单
- **逾期任务 fresh start**：冒险页逾期不堆成「羞耻墙」——折叠为一行带数量、点击展开、一键归草丛清截止时间（反羞耻）
- **桌宠**：番茄钟（时长/短休息/通知可配）、专注模式、快捷图鉴屏、换装
- **桌宠体验升级（环境感知层）**：输入响应（rdev 全局监听只计数、每秒喂 `{cps}` 不取按键内容；macOS 辅助功能权限引导 + 拒绝回落；专注态可选节奏轻耦合 ≤+10%，默认关）、AI 对话气泡（右键菜单入口跟随 agent 配置，`pet_chat` 接主 agent + 任务上下文，≤2 句只答不执行，对话框原位对话态、Esc 收起）、时刻台词（整点/深夜/周五晚/精灵生日，每类每周期一次，urgent/提醒让路）、屏幕边缘栖息（拖到底缘任务栏一带切坐/趴、动画幅度减半）、连胜捕捉演出（连续捕捉日 + 一个宽容日、今天不清零；≥3 天当日首捕触发双球交替三摇，无角标）、陪跑精灵（专注 ≥25 分钟图鉴精灵来旁陪坐，一次会话一位一次、零台词零数值）、桌宠语音（平台系统 TTS：say/System.Speech/spd-say，四类「郑重时刻」各一句，默认静音；EdgeTTS 云档留作后续）——方案见 [PET_EXPERIENCE_PROPOSAL](proposals/PET_EXPERIENCE_PROPOSAL.md)
- **就近可操作提醒**：桌宠提醒气泡直接带「✔ 完成 / ⇨ 推迟 10 分钟」，60 秒内就近消化，无需打开主面板
- **交互收敛**：「加入路线」操作（表单时间选择与卡片 📅）收进草丛页，其他页面快速添加一律先进草丛；移除桌宠操作按钮的悬浮提示
- **环境化倒计时**：桌宠随剩余时间渐变的色环（绿→琥珀→红）+ 尾段精灵焦急加速；长番茄钟（≥45 分钟）中点与剩 5 分钟轻提示音（可关）
- **「先试 5 分钟」启动模式**：桌宠 🍦 按钮开一场「不成也没关系」的超短专注；到期不接休息，继续/收工都被肯定（零挫败入场与退出门票）
- **每周复盘（训练家复盘）**：图鉴页五步向导——总览统计/路线逐站就地处置（保留·完成·归草丛·逃走）/草丛批量放生/标签体检（近义合并·僵尸放生·新维度建议）/收尾记录；设置可选提醒日、到点系统通知每周一次（OmniFocus 式回顾 + 提醒的差异化组合）
- **数据安全**：秘钥（飞书 App Secret / OAuth token 等，后随引擎下线）迁 OS 钥匙串（Keychain / Credential Manager / Secret Service），启动自动迁移存量明文，无钥匙串环境回落本地库；后端不回传明文；已下线集成的遗留键（builtin 飞书引擎 / Todoist Token）启动时自动清理
- **自动版本化备份**：`VACUUM INTO` 每日快照（滚动保留可配，默认 7 份）+ 设置页手动备份/列表/在线恢复（SQLite backup API 灌回，免重启）
- **导出/导入三件套**：全量 JSON（可回导、秘钥不出文件）、任务 CSV（BOM + RFC4180 转义）、日报 Markdown（已完成/路线上/今日跟进，勾选框语法可并入知识库）
- **常驻应用标配**：单实例、系统托盘、全局快捷键快速捕捉（`Ctrl/Cmd+Shift+K`）与显隐桌宠、窗口位置记忆、minisign 签名自动更新
- **工程基建**：SQLite 迁移机制（`PRAGMA user_version`）、统一错误处理、假 CLI/wiremock（后者随 HTTP 集成下线移除）覆盖外部链路、三引擎 E2E + Rust 契约测试、CI（clippy / fmt / eslint / lefthook / release-please / Renovate）、社区文件（issue / PR 模板、CONTRIBUTING、SECURITY）、VS Code 一键调试（lldb-dap：桌面应用 + 前端组合启动）、pk sidecar 三平台打包链路（Windows `.exe` 命名、macOS universal 双架构 lipo）

---

## 🚫 明确暂不做

- **Live2D**：CPU 占用高（tauri-live2d 作者因此弃坑），GIF/帧动画够用且省电
- **日历双向同步**：循环任务的双向同步是 TickTick / Todoist 的事故重灾区；只读 ICS 已在 Later
- **惩罚性游戏机制**：掉血 / 枯树式愧疚设计——只做正向激励（Finch 模式，非 Habitica 惩罚模式）
- **任务养成闭环**：亲密度 / EXP / 喂食 / 心情体力条这类「需要伺候宠物」的机制——桌宠是镜子不是闹钟（PET_INTERACTION_DESIGN 设计立场），陪伴不能变成注意力税；激励只走捕捉演出与图鉴收集
- **云端账号体系 / 云同步**：本地优先是根基，数据不出本机
- **默认开启的遥测**：若未来加入匿名统计，默认关闭 + 显式 opt-in + 逐事件枚举
- **RTL / 完整键盘无障碍**：当前品类普遍空白；有需求欢迎开 issue 推动
